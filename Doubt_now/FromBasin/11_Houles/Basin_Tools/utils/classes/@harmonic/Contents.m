% @HARMONIC
%
% Files
%   convert2dim    - harmonic = convert2dim(harmonic, depth)
%   convert2nondim - harmonic = convert2nondim(harmonic, depth)
%   display        - display(harmonic,name,tab)
%   get            - val = get(s,propName)
%   harmonic       - wv = harmonic(harmo, ampli, phase, angle)
%   isempty        - TF = isempty(A)
%   length         - y = length(A)
%   minus          - harmo = minus(harmo1,harmo2)
%   mtimes         - harmonic = mtimes(alpha,harmonic)
%   plus           - harmonic_out = plus(harmonic1,harmonic2)
%   rotate         - harmonic_in = rotate(harmonic_in, theta)
%   set            - s = set(s,varargin)
%   subsasgn       - b = subsasgn(s,index,val)
%   subsref        - b = subsref(s,index)
%   times          - harmonic_in = times(alpha,harmonic_in)
%   uminus         - harmo = uminus(harmo1)
